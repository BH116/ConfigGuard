// Sample broken configuration for AgentGuard test fixtures. All secret-shaped values are non-functional placeholders.
{
  "name": "CustomerSupportBot",
  "permissions": "*",
  "tools": [
    { "name": "send_email" },
    { "name": "update_account" },
    { "name": "query", "sql": "SELECT * FROM customers" }
  ],
  "mcpServers": {"support": {"command": "node support.js"}},
  "tenant": "workspace",
  "chat": true
}
