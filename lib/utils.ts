// ConfigGuard
/** Numeric sort order for finding severities, where lower numbers indicate more severe findings (critical first). */
export const severityOrder = { critical: 0, high: 1, medium: 2, low: 3, info: 4 } as const;
